namespace StackImpl
{
    internal class Stacks
    {
        static void Main()
        {
            // https://en.wikipedia.org/wiki/Stack_(abstract_data_type)
            // pila -> Fifo (First In First Out)
            /*

            enqueue	O(1)	Add an item to the top of the queue
            dequeue	O(1)	Remove and return the bottom item from the queue
            peek	O(1)	Return the top item from the queue without modifying the queue
            size	O(1)	Return the number of items in the queue


            getting to an item at the bottom of the queue have a higher time complexity because they require multiple dequeue operations.
            actually is O(n)
            Queue operations are limited: no searching, no sorting, no random access

            El problema de las colas es que no se puede acceder a un elemento en particular,
            solo al primero y al eliminar este se mueven todos los elementos hacia abajo, lo que hace que el tiempo de acceso a un elemento en particular sea O(n).

            It's okay to violate academic constraints as software engineers
            as long as we understand the trade-offs involved.

            head(derecha) -> first element in the queue
            tail(izquierda) -> last element in the queue



            */



        }
    }
}