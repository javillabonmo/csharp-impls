@* Validación nativa del navegador — sin dependencias externas. *@
@* El formulario usa HTML5 validation attributes (asp-for genera required, data-val-*). *@
@* Para personalizar, escribe tu propio JS en wwwroot/js/site.js. *@
