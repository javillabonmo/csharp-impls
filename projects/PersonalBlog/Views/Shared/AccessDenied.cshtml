@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container">
    <div class="row justify-content-center" style="padding-top: 6rem;">
        <div class="col-md-6 col-lg-5">
            <div class="glass-card p-5 text-center">
                <div class="display-1 fw-bold text-gradient mb-2">403</div>
                <h3 class="fw-bold mb-2" style="color: var(--text-dark);">Acceso Denegado</h3>
                <p class="mb-4" style="color: var(--text-soft);">No tienes permisos para acceder a esta página.</p>
                <a asp-controller="Home" asp-action="Index" class="btn-glass">Volver al inicio</a>
            </div>
        </div>
    </div>
</div>
